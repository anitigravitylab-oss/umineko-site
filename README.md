# うみねこ公式サイト
依存・ビルド不要の静的サイト。公開先: https://umineko.dev
ページと画像・スタイルは `public/`、配信設定は `wrangler.toml` にあります。
ローカル確認: `wrangler dev --port 8799`（Cloudflare Workers の静的アセット配信）。
公開はオーナーが行います。この作業ではデプロイしていません。
